import {Text, View, ScrollView} from "react-native"

export default ({navigation}) => {
    return (
        <ScrollView>
            <Text>TRACK UR FUCKING CALORIES</Text>
        </ScrollView>
    )
}